# The Next.js app now sits at the repository root, so no base directory is
# needed. `output: "export"` in next.config.ts writes a plain static site to
# out/, which any host can serve directly.
[build]
  command = "npm run build"
  publish = "out"

[build.environment]
  NODE_VERSION = "22"
